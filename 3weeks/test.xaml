<Window x:Class="MT.ExtractorToCSV.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MT.ExtractorToCSV"
        mc:Ignorable="d"
        Title="MainWindow" 
        Height="800" Width="1400"
        Background="Black"
        d:DataContext="{d:DesignInstance Type=local:MainWindow}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Margin="5"/>
            <Button Grid.Column="1" Content="Open" Margin="5" Click="btnOpen_Click"/>
            <Button Grid.Column="2" Content="변환" Margin="5" Click="btnConvert_Click"/>
        </Grid>
        <Grid Grid.Row="1" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <!-- 좌측: 헤더 , TreeView -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="0.03*"/>
                    <RowDefinition Height=  "*"/>
                </Grid.RowDefinitions>
                <!-- 헤더 -->
                <Grid Grid.Row="0" Margin="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="55"/>
                        <ColumnDefinition Width="40"/>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="160"/>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="80"/>
                    </Grid.ColumnDefinitions>
                    <!-- 선택 헤더를 CheckBox로 변경 -->
                    <CheckBox Grid.Column="0"
                              VerticalAlignment="Center"
                              HorizontalAlignment="Center"
                              Padding="0,4"
                              Name="chkAllSelect"
                              Checked="chkAllSelect_Checked"
                              Unchecked="chkAllSelect_Unchecked"/>
                    <TextBlock Text="Level" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold" Foreground="White" Padding="0,4"/>
                    <TextBlock Text="Type" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold" Foreground="White" Padding="0,4"/>
                    <TextBlock Text="Name" Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Left" FontWeight="Bold" Foreground="White" Padding="4,4,0,4"/>
                    <TextBlock Text="TargetPath" Grid.Column="4" VerticalAlignment="Center" HorizontalAlignment="Left" FontWeight="Bold" Foreground="White" Padding="4,4,0,4"/>
                    <TextBlock Text="FileSize(MB)" Grid.Column="5" VerticalAlignment="Center" HorizontalAlignment="Right" FontWeight="Bold" Foreground="White" Padding="0,4,4,4"/>
                    <TextBlock Text="Progress" Grid.Column="6" VerticalAlignment="Center" HorizontalAlignment="Right" FontWeight="Bold" Foreground="White" Padding="0,4,4,4"/>
                </Grid>
                <!-- TreeView -->
                <TreeView Grid.Row="1" Name="tvSource"
                          ItemsSource="{Binding SourceList, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:MainWindow}}"
                          Margin="0,5,0,0"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Items}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="55"/>
                                    <ColumnDefinition Width="165"/>
                                    <ColumnDefinition Width="190"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="100"/>
                                </Grid.ColumnDefinitions>
                                <CheckBox IsChecked="{Binding IsChecked}" Grid.Column="0"/>
                                <TextBlock Text="{Binding Level}" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Type}" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Name}" Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Left" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding TargetPath}" Grid.Column="4" VerticalAlignment="Center" HorizontalAlignment="Left" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding FileSizeMb}" Grid.Column="5" VerticalAlignment="Center" HorizontalAlignment="Right" TextAlignment="Right"/>
                                <ProgressBar Grid.Column="6" Height="16" Margin="4,0,0,0"
                                             Minimum="0" Maximum="100"
                                             Value="{Binding Progress}" />
                            </Grid>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </Grid>
            <Border Grid.Column="1" Background="Black" Width="1" Margin="4,0" HorizontalAlignment="Center"/>
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="60"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Grid.RowSpan="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Output" VerticalAlignment="Center" HorizontalAlignment="Left" FontWeight="Bold" Foreground="White" Margin="5,0,0,0"/>
                </Grid>
                <TreeView Name="tvOutput" Margin="0,26,0,0" Grid.RowSpan="2"/>
            </Grid>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="60"/>
            </Grid.ColumnDefinitions>
        </Grid>
    </Grid>
</Window>
